//Import Actions
import {} from "../actions/types";

const initialState = {};

export default function (state = initialState, action) {
  switch (action.type) {
    //Handle different cases
    default:
      return state;
  }
}
